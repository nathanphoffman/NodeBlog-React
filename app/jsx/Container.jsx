var React = require('react');
var Preview = require('./Container/Preview.jsx');
module.exports = class extends React.Component {
    render() {
        // rocket (Space)      
        // space-shuttle (Exploration) 
        // moon-o (Stargazing)     
        // calculator (Math)
        // flask (Science)

        //coding:
        // file-code-o (Coding)
        // mobile (Mobile)
        // chrome (Web)
        // floppy-o (Application)

        // photo (Art)
        // paint-brush (Painting)
        // camera (Photos)


        return (

        );
    }
};